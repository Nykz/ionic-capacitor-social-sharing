import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.technyks.socialSharing',
  appName: 'Social Sharing App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
